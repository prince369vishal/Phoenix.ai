# 'mock' (default) runs the demo entirely in the browser against fixture data.
# 'api' points the same UI at a running @phoenix-ai/api instance.
VITE_DATA_SOURCE=mock

# Only used when VITE_DATA_SOURCE=api
VITE_API_BASE_URL=http://localhost:3001
