# Runbooks

Operational runbooks for running the platform against a system under analysis: onboarding a
new system, granting/scoping connector access, handling a failed/partial ingestion run,
responding to a drift-detection alert, and rotating credentials for an isolated execution
environment.

## Status

No runbooks written yet. Add one file per operational scenario as the platform reaches
production readiness (e.g. `onboard-new-system.md`, `handle-ingestion-failure.md`,
`respond-to-drift-alert.md`).
