{
  "name": "@phoenix-ai/inference-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Interprets verified facts into domains, bounded contexts, and rationale using AI over the deterministic skeleton — never over raw files.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "lint": "eslint src",
    "typecheck": "tsc -b --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@phoenix-ai/shared": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.4"
  }
}
