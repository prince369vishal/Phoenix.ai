/**
 * @phoenix-ai/review-loop
 * Module: Human Review & Correction Loop
 *
 * Scaffold entry point. Replace with the module's real public API.
 * See the FRD for functional requirements and the module's README for
 * responsibility and trust-boundary notes.
 */

export const MODULE_NAME = 'review-loop' as const;

export function placeholder(): string {
  return `${MODULE_NAME} module scaffold — implementation pending`;
}
